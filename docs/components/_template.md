# {コンポーネント名}

> **ステータス**: ⚠️ ドラフト
> この Spec は未確定。実装着手前に [`sdd-workflow.md`](../process/sdd-workflow.md) §7 のドキュメント作成サイクル（ヒアリング → ドラフト → レビュー → 確定）を通すこと。

<!--
このファイルは sazare-ui の全コンポーネント Spec の雛形である。
新しいコンポーネントを追加するときは、本ファイルをコピーして
`docs/components/<component-name>.spec.md` を作成し、各章を埋める。

運用ガイド:
- 章タイトル・章順序を変更しない。`docs/README.md` §「Spec テンプレートの確定項目」と 1:1 で対応する。
- 「関連コンポーネント・依存」章は意図的に存在しない（同上）。
- 表の各行に付与する「テスト ID」は、§13 テストシナリオの見出しと完全一致させる。命名規則は以下:
  - `T-state-NN` … 状態（§4）
  - `T-interaction-NN` … インタラクション（§5）
  - `T-variant-NN` … バリアント（§6）
  - `T-a11y-NN` … アクセシビリティ（§7）
  - `T-key-NN` … キーボード操作（§8）
  - `T-inv-NN` … 不変条件（§9）
  - `T-edge-NN` … 例外ケース（§10）
- スタイル値はすべて `tokens/*.md` のトークン経由で指定する。リテラル値（`#FFF`, `4px` 等）の直書きは禁止。
- 記述不要の章・行は空欄にせず、明示的に「該当なし」と書く（**全章に共通適用**。抜けと意図的省略を区別するため）。
- 完成後は冒頭の「⚠️ ドラフト」ラベルを「確定（YYYY-MM-DD）」に更新する。
- **前提**: 本テンプレートを使った Spec 作成は、`principles/*.md` および `tokens/*.md`（Phase 0 着手順 #5〜#15 参照）が揃ってから着手する。これらが未確定だと §7 アクセシビリティ・§12 デザイントークンのマッピングが空欄になりやすい。
-->

---

## 1. 概要

<!--
このコンポーネントが何を解決するか、どのユースケースを想定しているかを 1〜3 段落で記述する。
- 何をするコンポーネントか（1 文）
- 想定される利用シーン
- 関連する原則（design-principles.md / accessibility.md 等）への参照
-->

{{1〜3 段落でコンポーネントの目的と利用シーンを記述する}}

---

## 2. Anatomy

<!--
コンポーネントの構造を視覚的・階層的に示す。
- ツリー表記でルートから子要素までの階層を表現
- 各パートに識別名を付ける（例: Root, Icon, Label）
- compound component の場合はサブコンポーネントを明示
- optional パートは `(optional)` と注記
-->

```
{{Component}}
├── Root
│   ├── {{Part1}}
│   └── {{Part2}} (optional)
```

---

## 3. Props API

<!--
Props は表形式で記述する。`principles/api-design.md` の命名規則に従うこと。
- 型は TypeScript 型表現で記述
- デフォルト値があれば明示。なければ「-」
- 必須/任意を明確に
- イベントコールバック（onClick 等）は「説明」欄に発火条件を書き、§5 インタラクションで T-interaction-NN として 1:1 でテスト化する（状態が変わる場合は §4 状態でも併記）
-->

| Prop | 型 | デフォルト | 必須 | 説明 |
|---|---|---|---|---|
| {{name}} | {{type}} | {{default}} | {{yes/no}} | {{description}} |

---

## 4. 状態

<!--
コンポーネントが取りうる視覚的・論理的状態を列挙する。
- 各行 = 1 テストケース（sdd-workflow.md §3.4 の 1:1 翻訳ルール）
- ID 列: §13 の見出しと完全一致させる（例: T-state-01）
- トリガー: その状態に遷移する条件（Props・ユーザー操作・コンテキスト）
- 期待される表示・挙動: 視覚と振る舞いを 1 文で
- 関連トークン: そのフェーズで使うデザイントークン（カンマ区切り可）。§12 と整合させる

表形式で書ききれない補足のみ ### 補足: <state name> として個別に追加する。
-->

| ID | 状態 | トリガー | 期待される表示・挙動 | 関連トークン |
|---|---|---|---|---|
| T-state-01 | {{state}} | {{trigger}} | {{behavior}} | {{tokens}} |

---

## 5. インタラクション

<!--
ユーザー操作（クリック・タッチ・ポインタ移動・フォーカス変化など、キーボード以外）と、それが引き起こす副作用を列挙する。
- 各行 = 1 テストケース（sdd-workflow.md §3.4 の 1:1 翻訳ルール）
- 状態が変わる操作は §4 状態にも記載し、両方の表で同一トリガーを参照する（重複ではなく観点が異なる：§4 は「結果として何状態か」、§5 は「副作用として何が起きるか」）
- キーボード固有の操作は §8 キーボード操作で扱う
- コールバック発火・ARIA 属性更新・フォーカス移動・親への通知 など、副作用を「期待される挙動」に明記する

該当がない場合は「該当なし」と明示する（純粋な表示専用コンポーネントなど）。
-->

| ID | ユーザー操作 | コンテキスト | 期待される挙動 |
|---|---|---|---|
| T-interaction-01 | {{action}} | {{when}} | {{behavior + side effects}} |

---

## 6. バリアント

<!--
バリアント = 同じコンポーネントの「種類」軸（size / intent / variant 等）。
状態（default/hover/disabled）とは別軸であることに注意。

- バリアント軸ごとに ### で見出しを切る
- 軸が 1 つしかない場合も明示的に軸名を書く
- 「該当なし」の場合はその旨を記載
-->

### 6.1 {{バリアント軸名（例: variant）}}

| ID | 値 | 用途 | デザイン差分 | 関連トークン |
|---|---|---|---|---|
| T-variant-01 | {{value}} | {{use case}} | {{visual diff}} | {{tokens}} |

---

## 7. アクセシビリティ

<!--
WCAG 準拠レベルとこのコンポーネント固有の a11y 要件を表で記述する。
- 詳細な原則は principles/accessibility.md を参照
- 各行 = 1 a11y テストケース
- WCAG 列には基準番号と準拠レベル（A / AA / AAA）を記載
- テスト方法列にはツール・チェック観点を簡潔に記載（例: axe / 手動: スクリーンリーダー）
-->

| ID | 要件 | WCAG | テスト方法 |
|---|---|---|---|
| T-a11y-01 | {{requirement}} | {{ref + level}} | {{tool / method}} |

---

## 8. キーボード操作

<!--
各キーバインドが 1 行 = 1 テストケースに対応する。
principles/accessibility.md のキーボード操作基準と整合させる。
- コンテキスト: フォーカスがどこにあるか・どの状態のときか
- 修飾キー併用は「Shift + Tab」のように記載
-->

| ID | キー | コンテキスト | 期待される挙動 |
|---|---|---|---|
| T-key-01 | {{key}} | {{when}} | {{behavior}} |

---

## 9. 不変条件

<!--
このコンポーネントが「常に満たすべき」性質を列挙する。
- 1 つの不変条件 = 1 assertion でテスト可能な粒度に分割する
- 可能であれば TypeScript の型制約・assertion 関数で表現する（sdd-workflow.md §3.3）
- 「ある状態が同時に成立しない」など否定形でも可
-->

| ID | 不変条件 |
|---|---|
| T-inv-01 | {{invariant statement}} |

---

## 10. 例外ケース

<!--
通常フローから外れる状況とその扱いを記述する。例:
- 不正な props の組み合わせ
- 必要な親コンテキストが不在
- 非同期処理中のアンマウント
- 子要素が 0 件 / 上限超過

該当がない場合は「該当なし」と明示する（空欄にしない）。
-->

| ID | ケース | 扱い |
|---|---|---|
| T-edge-01 | {{case}} | {{handling}} |

---

## 11. 使用例

<!--
最小例から始めて、典型的なバリアント・compound 利用例の順に並べる。
- コードはコピー & ペーストで動くものを推奨
- import は省略可
- スタイル値は必ずトークン経由（生 CSS の例を書かない）
-->

### 11.1 最小例

```tsx
<{{Component}}>{{...}}</{{Component}}>
```

### 11.2 {{典型バリアント例}}

```tsx
<{{Component}} variant="...">{{...}}</{{Component}}>
```

---

## 12. デザイントークンのマッピング

<!--
このコンポーネントが使う tokens/*.md 内のトークンを列挙する。
- 「用途」: そのトークンが何に使われるか（背景色 / 角丸 / フォントサイズ など）
- 「トークン」: tokens/*.md で定義された正式名（kebab-case またはドット記法）
- 「参照」: 該当トークンが定義されているファイル

ここに列挙されていないリテラル値の使用は実装段階で禁止する。
新しいトークンが必要になった場合は tokens/*.md 側で先に定義してから本表に追加する。
-->

| 用途 | トークン | 参照 |
|---|---|---|
| {{purpose}} | {{token name}} | `tokens/{{file}}.md` |

---

## 13. テストシナリオ

<!--
§4〜§10 の各表で付与した ID に対応する Given / When / Then を記述する。
これが Spec ↔ テスト 1:1 翻訳の出口（sdd-workflow.md §3.4）。

- 見出しは「### {{ID}}: {{要約}}」の形式に統一する
- ID の並び順: T-state-* → T-interaction-* → T-variant-* → T-a11y-* → T-key-* → T-inv-* → T-edge-*
- 表に記載した ID と見出しは完全一致させる（欠けると 1:1 翻訳が崩れる）
- Given / When / Then は各 1 行〜数行で簡潔に
-->

### T-state-01: {{要約}}

- **Given**: {{前提条件}}
- **When**: {{操作・状態変化のトリガー}}
- **Then**: {{期待される結果}}

### T-interaction-01: {{要約}}

- **Given**: {{前提条件}}
- **When**: {{ユーザー操作（クリック・タッチなど）}}
- **Then**: {{期待される結果（コールバック発火・ARIA 更新など）}}

### T-variant-01: {{要約}}

- **Given**: {{前提条件}}
- **When**: {{バリアントの適用}}
- **Then**: {{期待される結果}}

### T-a11y-01: {{要約}}

- **Given**: {{前提条件}}
- **When**: {{a11y チェックの実行}}
- **Then**: {{期待される結果}}

### T-key-01: {{要約}}

- **Given**: {{前提条件（フォーカス位置など）}}
- **When**: {{キー入力}}
- **Then**: {{期待される結果}}

### T-inv-01: {{要約}}

- **Given**: {{前提条件}}
- **When**: {{任意の操作 / 任意の Props 組み合わせ}}
- **Then**: {{不変条件が常に保たれる}}

### T-edge-01: {{要約}}

- **Given**: {{前提条件}}
- **When**: {{例外を誘発する操作 / 入力}}
- **Then**: {{期待される結果（フォールバック・エラー表示など）}}

---

## 改訂履歴

| 日時 | 内容 |
|---|---|
| YYYY-MM-DD | 初版ドラフト |
| YYYY-MM-DD | レビュー反映、確定 |
